@{
    var site = await WebApp.Api.Sites.GetDefaultAsync();
    var siteContent = await WebApp.Site.GetContentAsync<OpenEditorial.Models.JournalSite>();
}

<div class="container">
    @*<div class="row align-items-center">
        @if (site.Logo.HasValue)
        {
            <div class="col-2">
                <img src="@Url.Content(site.Logo)" />
            </div>
        }
        <div class="col-6">
            <p class="h3"><a>@siteContent.Info.Title</a></p>
        </div>
        <div class="col-2 offset-2">
            @if (!String.IsNullOrWhiteSpace(siteContent.Info.ISSN.Value))
            {
                <div>ISSN: @siteContent.Info.ISSN</div>
            }
        </div>
    </div>*@
    <div class="d-flex flex-row align-items-center pt-1">
        @if (site.Logo.HasValue)
        { 
            <div>
                <img src="@Url.Content(site.Logo)" />
            </div>
        }
        <div class="pl-5 text-primary">
            <h3 class="journal-title"><a>@siteContent.Info.Title</a></h3>
        </div>
        <div class="ml-auto mr-1">
            @if (!String.IsNullOrWhiteSpace(siteContent.Info.ISSN.Value))
            {
                <div>ISSN: @siteContent.Info.ISSN</div>
            }
            <div>
                <a href="~/manager">Войти</a>
            </div>
        </div>
    </div>
</div>
